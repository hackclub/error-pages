# error-pages

This repo hosts Hack Club branded error pages. This works with both Coolify and Cloudflare. All cloudflare error pages are in the `cloudflare` folder, and all Coolify error pages are in the root folder. The error pages are designed to be as similar as possible, but there may be some differences due to the different platforms and what data is passed to the error pages.